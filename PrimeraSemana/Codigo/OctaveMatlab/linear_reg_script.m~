%function ReadAndRunLinearRegresion
clear all;
close all;
clc;
data = load('../../Datos/univariate_reg_data.txt');
X = data(:, 1); y = data(:, 2);
plot(X, y, '*');
X = (X - min(X))
size(X)
size(y)
gradient_descent_one_var(X, y, 0.005, 1000)